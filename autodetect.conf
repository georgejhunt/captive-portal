
server {
    listen 80;
    server_name captive.apple.com;
    location ~* ^.*$ {
	   include uwsgi_params;
	   uwsgi_pass unix:///tmp/autoportal.sock;
    }
}
server {
    listen 80;
    server_name appleiphonecell.com;
    location ~* ^.*$ {
	   include uwsgi_params;
	   uwsgi_pass unix:///tmp/autoportal.sock;
    }
}
server {
    listen 80;
    server_name *.apple.com.edgekey.net;
    location ~* ^.*$ {
	   include uwsgi_params;
	   uwsgi_pass unix:///tmp/autoportal.sock;
    }
}
server {
    listen 80;
    server_name gsp1.apple.com;
    location ~* ^.*$ {
	   include uwsgi_params;
	   uwsgi_pass unix:///tmp/autoportal.sock;
    }
}
server {
    listen 80;
    server_name apple.com;
    location ~* ^.*$ {
	   include uwsgi_params;
	   uwsgi_pass unix:///tmp/autoportal.sock;
    }
}
server {
    listen 80;
    server_name www.apple.com;
    location ~* ^.*$ {
	   include uwsgi_params;
	   uwsgi_pass unix:///tmp/autoportal.sock;
    }
}
server {
    listen 80;
    server_name iphone-ld.apple.com;
    location ~* ^.*$ {
	   include uwsgi_params;
	   uwsgi_pass unix:///tmp/autoportal.sock;
    }
}
server {
    listen 80;
    server_name netcts.cdn-apple.com;
    location ~* ^.*$ {
	   include uwsgi_params;
	   uwsgi_pass unix:///tmp/autoportal.sock;
    }
}
server {
    listen 80;
    server_name ipv6.msftncsi.com;
    location ~* ^.*$ {
	   include uwsgi_params;
	   uwsgi_pass unix:///tmp/autoportal.sock;
    }
}
server {
    listen 80;
    server_name detectportal.firefox.com;
    location ~* ^.*$ {
	   include uwsgi_params;
	   uwsgi_pass unix:///tmp/autoportal.sock;
    }
}
server {
    listen 80;
    server_name ipv6.msftncsi.com.edgesuite.net;
    location ~* ^.*$ {
	   include uwsgi_params;
	   uwsgi_pass unix:///tmp/autoportal.sock;
    }
}
server {
    listen 80;
    server_name www.msftncsi.com;
    location ~* ^.*$ {
	   include uwsgi_params;
	   uwsgi_pass unix:///tmp/autoportal.sock;
    }
}
server {
    listen 80;
    server_name www.msftncsi.com.edgesuite.net;
    location ~* ^.*$ {
	   include uwsgi_params;
	   uwsgi_pass unix:///tmp/autoportal.sock;
    }
}
server {
    listen 80;
    server_name www.msftconnecttest.com;
    location ~* ^.*$ {
	   include uwsgi_params;
	   uwsgi_pass unix:///tmp/autoportal.sock;
    }
}
server {
    listen 80;
    server_name www.msn.com;
    location ~* ^.*$ {
	   include uwsgi_params;
	   uwsgi_pass unix:///tmp/autoportal.sock;
    }
}
server {
    listen 80;
    server_name teredo.ipv6.microsoft.com;
    location ~* ^.*$ {
	   include uwsgi_params;
	   uwsgi_pass unix:///tmp/autoportal.sock;
    }
}
server {
    listen 80;
    server_name teredo.ipv6.microsoft.com.nsatc.net;
    location ~* ^.*$ {
	   include uwsgi_params;
	   uwsgi_pass unix:///tmp/autoportal.sock;
    }
}
server {
    listen 80;
    server_name ctldl.windowsupdate.com;
    location ~* ^.*$ {
	   include uwsgi_params;
	   uwsgi_pass unix:///tmp/autoportal.sock;
    }
}
server {
    listen 80;
    server_name clients3.google.com;
    location ~* ^.*$ {
	   include uwsgi_params;
	   uwsgi_pass unix:///tmp/autoportal.sock;
    }
}
server {
    listen 80;
    server_name mtalk.google.com;
    location ~* ^.*$ {
	   include uwsgi_params;
	   uwsgi_pass unix:///tmp/autoportal.sock;
    }
}
server {
    listen 80;
    server_name alt7-mtalk.google.com;
    location ~* ^.*$ {
	   include uwsgi_params;
	   uwsgi_pass unix:///tmp/autoportal.sock;
    }
}
server {
    listen 80;
    server_name alt6-mtalk.google.com;
    location ~* ^.*$ {
	   include uwsgi_params;
	   uwsgi_pass unix:///tmp/autoportal.sock;
    }
}
server {
    listen 80;
    server_name connectivitycheck.android.com;
    location ~* ^.*$ {
	   include uwsgi_params;
	   uwsgi_pass unix:///tmp/autoportal.sock;
    }
}
server {
    listen 80;
    server_name connectivitycheck.gstatic.com;
    location ~* ^.*$ {
	   include uwsgi_params;
	   uwsgi_pass unix:///tmp/autoportal.sock;
    }
}
server {
    listen 80;
    server_name developers.google.cn;
    location ~* ^.*$ {
	   include uwsgi_params;
	   uwsgi_pass unix:///tmp/autoportal.sock;
    }
}
server {
    listen 80;
    server_name connectivity-check.ubuntu.com;
    location ~* ^.*$ {
	   include uwsgi_params;
	   uwsgi_pass unix:///tmp/autoportal.sock;
    }
}
server {
    listen 80;
    server_name nmcheck.gnome.org;
    location ~* ^.*$ {
	   include uwsgi_params;
	   uwsgi_pass unix:///tmp/autoportal.sock;
    }
}
server {
    listen 80;
    server_name detectportal.firefox.com;
    location ~* ^.*$ {
	   include uwsgi_params;
	   uwsgi_pass unix:///tmp/autoportal.sock;
    }
}
